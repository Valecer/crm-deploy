# Инструкция по настройке деплоя в Timeweb Cloud

## 📋 Созданные приложения

### Frontend приложение
- **Название**: CRM-Frontend
- **URL**: https://valecer-crm-deploy-ae97.twc1.net
- **IP**: 46.19.64.95
- **Пресет**: Frontend app (1₽/мес)

### Backend приложение
- **Название**: CRM-Backend
- **URL**: https://valecer-crm-deploy-c126.twc1.net
- **IP**: 72.56.76.161
- **Пресет**: Backend app (250₽/мес)

---

## 🔧 Настройка переменных окружения

### Backend приложение

В панели управления Timeweb Cloud перейдите в раздел вашего **Backend приложения** → **Переменные окружения** и добавьте следующие переменные:

#### Обязательные переменные:

1. **JWT_SECRET** (ОБЯЗАТЕЛЬНО!)
   ```
   Значение: [сгенерируйте надежный секретный ключ, минимум 32 символа]
   Пример: JWT_SECRET=your-super-secret-key-change-this-in-production-min-32-chars
   ```
   ⚠️ **Важно**: Используйте надежный случайный ключ для безопасности!

2. **NODE_ENV**
   ```
   Значение: production
   ```

3. **PORT** (опционально, по умолчанию 5174)
   ```
   Значение: 5174
   ```

4. **DB_PATH** (опционально, путь к базе данных SQLite)
   ```
   Значение: /app/data/database.sqlite
   ```

#### Рекомендуемые переменные:

5. **FRONTEND_URL** (для CORS)
   ```
   Значение: https://valecer-crm-deploy-ae97.twc1.net
   ```
   Это разрешит запросы с frontend домена.

6. **JWT_EXPIRES_IN** (опционально, по умолчанию 7d)
   ```
   Значение: 7d
   ```

7. **SUPPORT_PHONE_NUMBER** (опционально)
   ```
   Значение: +7-XXX-XXX-XX-XX
   ```

8. **SUPPORT_EMAIL** (опционально)
   ```
   Значение: support@example.com
   ```

### Frontend приложение

В панели управления Timeweb Cloud перейдите в раздел вашего **Frontend приложения** → **Переменные окружения** и добавьте:

1. **VITE_API_BASE_URL**
   ```
   Значение: https://valecer-crm-deploy-c126.twc1.net
   ```
   Это URL вашего backend приложения (без `/api` в конце).

---

## 🔄 Обновление приложений после изменений

После настройки переменных окружения:

1. **Пересоберите Frontend** (если изменили переменные окружения):
   - В панели Timeweb Cloud перейдите в Frontend приложение
   - Нажмите "Пересобрать" или "Redeploy"

2. **Перезапустите Backend**:
   - В панели Timeweb Cloud перейдите в Backend приложение
   - Нажмите "Перезапустить"

---

## 📝 Проверка работоспособности

### Проверка Backend:
1. Откройте в браузере: https://valecer-crm-deploy-c126.twc1.net/health
   - Должен вернуться: `{"status":"ok"}`

2. Откройте: https://valecer-crm-deploy-c126.twc1.net/api
   - Должен вернуться: `{"message":"Server Support CRM API"}`

### Проверка Frontend:
1. Откройте в браузере: https://valecer-crm-deploy-ae97.twc1.net
   - Должна загрузиться главная страница

2. Проверьте консоль браузера (F12) на наличие ошибок подключения к API

---

## 🔐 Генерация JWT_SECRET

Для генерации надежного секретного ключа используйте одну из команд:

### Linux/macOS:
```bash
openssl rand -base64 32
```

### Node.js:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

### Онлайн генератор:
- Используйте любой надежный генератор случайных строк (минимум 32 символа)

---

## ⚠️ Важные замечания

1. **JWT_SECRET** - критически важная переменная для безопасности. Никогда не используйте дефолтное значение в продакшене!

2. **База данных** - SQLite база данных будет создана автоматически при первом запуске backend

3. **CORS** - Если frontend не может подключиться к backend, проверьте переменную `FRONTEND_URL` в backend

4. **Переменные окружения** - Изменения в переменных окружения требуют перезапуска приложения

---

## 🐛 Устранение проблем

### Backend не отвечает:
- Проверьте логи в панели Timeweb Cloud
- Убедитесь, что переменная `PORT` установлена правильно
- Проверьте, что база данных может быть создана (права на запись)

### Frontend не может подключиться к API:
- Проверьте переменную `VITE_API_BASE_URL` в frontend
- Убедитесь, что backend доступен по URL
- Проверьте CORS настройки в backend (переменная `FRONTEND_URL`)

### Ошибки авторизации:
- Проверьте, что `JWT_SECRET` установлен и не является дефолтным значением
- Убедитесь, что `JWT_SECRET` одинаковый при всех перезапусках backend

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи приложений в панели Timeweb Cloud
2. Убедитесь, что все переменные окружения настроены правильно
3. Проверьте доступность приложений по URL

---

**Дата создания**: 2024
**Версия**: 1.0

